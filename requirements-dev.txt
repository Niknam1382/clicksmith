-r requirements.txt

# The project itself, editable: without this, `clicksmith` isn't importable and every test
# and mypy fail immediately with a "module not found" error - this is not optional.
-e .

# Testing
pytest==8.3.4
coverage==7.6.10

# Static analysis
ruff==0.9.4
mypy==1.14.1

# Packaging
pyinstaller==6.20.0
